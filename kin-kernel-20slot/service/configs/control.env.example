KIN_CONTROL_ADDR=0.0.0.0:9090
KIN_HEARTBEAT_TIMEOUT_SECONDS=20
KIN_RECONCILE_INTERVAL_SECONDS=5
KIN_SNAPSHOT_TTL_SECONDS=3600

# No env var needed here: PUT/GET /api/v1/runtime-profile computes and serves
# the config_hash consumed by the kernel's KIN_DESIRED_CONFIG_HASH (see
# kernel.env.example) for the three-way integrity check (design.md §6).

